name: testing github actions

on:
  push:

jobs:
  test-job:
    runs-on: ubuntu-20.04
    steps:
      - name: print something
        run: echo ABC
      - name: checkout code
        uses: actions/checkout@v4
      
      - name: try print from composite action
        uses: ./action.yml